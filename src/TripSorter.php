<?php
namespace TripSorter;

/**
 * Class TripSorter
 *
 * @package TripSorter
 */
class TripSorter
{

    /** @var array */
    private $cards;

    /**
     * This method takes a json string as an argument, attempts to deserialize it and to build the
     * needed Card objects.
     *
     * @param string $jsonArray
     */
    public function loadBoardingCards(string $jsonArray): void
    {


    }
}